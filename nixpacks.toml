[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r web_agent/requirements.txt --break-system-packages"]

[start]
cmd = "cd web_agent && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
